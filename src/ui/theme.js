import { createMuiTheme } from "@material-ui/core/styles";

const palette = {
  primary: { main: "#E1F5FE" },
  secondary: { main: "#B3E5FC" },
};
const themeName = "Pattens Blue French Pass Rock Hyrax";

export default createMuiTheme({ palette, themeName });
